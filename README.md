# react-chengru-h5
react18+vite+zustand+antUI+ts+ReactRouter 商城类项目demo




**默认行为**：`.env.development` 里 `VITE_USE_MOCK=true` → 前端开箱即用（仍是 mock 数据）；要联调后端只需把 `VITE_USE_MOCK` 改成 `false`（`.env.test/.env.production` 已是 `false`）。